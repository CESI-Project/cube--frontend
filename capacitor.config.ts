import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cube.app',
  appName: 'cube--frontend',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
